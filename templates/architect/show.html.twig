{% extends 'base.html.twig' %}

{% block title %}Architect{% endblock %}

{% block body %}
    <h1>Architect</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ architect.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ architect.name }}</td>
            </tr>
            <tr>
                <th>Origin</th>
                <td>{{ architect.origin }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_architect_index') }}">back to list</a>

    <a href="{{ path('app_architect_edit', {'id': architect.id}) }}">edit</a>

    {{ include('architect/_delete_form.html.twig') }}
{% endblock %}
